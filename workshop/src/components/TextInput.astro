---
const { class: className, isDirty = false, isValid = false, ...rest } = Astro.props;
---
<input
  class:list={["input", className]}
  type="text"
  data-state={isDirty ? "dirty" : "clean"}
  data-validity={isValid ? "valid" : "invalid"}
  {...rest}
/>
