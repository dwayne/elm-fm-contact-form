---
const { class: className, value, isDirty = false, isValid = false, ...rest } = Astro.props;
---
<textarea
  class:list={["textarea", className]}
  data-state={isDirty ? "dirty" : "clean"}
  data-validity={isValid ? "valid" : "invalid"}
  {...rest}>{value}</textarea>
